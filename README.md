# 📋 Проект: Веб-форма с валидацией

Проект представляет собой адаптивную веб-форму для заполнения анкеты  с валидацией на стороне клиента и сервера.
Данные формы сохраняются в базу данных **MySQL**.  Реализованы практически все проверки, предусмотренные **техническим заданием**.

## 🚀 Быстрый запуск

### 🔧 Предварительные требования

- Установленный **Docker**
- Установленный **Docker Compose**

## ▶️ Инструкция по запуску

1. **Скачайте файлы проекта** с удалённого репозитория

2. **Настройте конфигурацию**
   - Переименуйте файл:
     ```bash
     sample.env → .env
     ```
   - *(Опционально)* отредактируйте настройки в файле `.env`
   
3. **Запустите проект в терминале**
   ```bash
   docker compose up -d
   ```
   
4. Дождитесь завершения установки всех зависимостей

## 🌐 Доступ к проекту
### 🖥️ Веб-приложение
1. Форма анкеты:
  - http://localhost:443
  - Порт по умолчанию: 443

### 🗄️ Администрирование базы данных
2. phpMyAdmin:
  - http://localhost:8080
  - Порт по умолчанию: 8080

## 🛢️ База данных
  - Название БД: grizzly_form
  - Таблица: requests
  - Данные из формы сохраняются автоматически в эту таблицу.
